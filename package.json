{
  "name": "everides",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "mobile",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace mobile",
    "dev:web": "npm run web --workspace mobile",
    "dev:mobile": "npm run dev --workspace mobile",
    "dev:server": "npm run dev --workspace server",
    "lint": "npm run lint --workspace shared && npm run lint --workspace server && npm run lint --workspace mobile",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace server && npm run typecheck --workspace mobile",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server"
  }
}
